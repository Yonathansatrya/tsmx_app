import '../shared/module_placeholder_screen.dart';
import '../../config/mobile_role_registry.dart';

/// Finance workspace — placeholder until Phase 4 implementation.
class FinanceMainScreen extends ModulePlaceholderScreen {
  const FinanceMainScreen({super.key})
      : super(moduleKey: MobileModule.finance);
}
